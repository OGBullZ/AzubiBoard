// Design-sync barrel — scopes the bundle to the exported UI primitives only.
export {
  Toast,
  StatusBadge,
  Avatar,
  Modal,
  Field,
  ProgressBar,
  StatCard,
  EmptyState,
  SectionHeader,
  ThemeToggle,
  IconBtn,
  Divider,
} from '../src/components/UI';
